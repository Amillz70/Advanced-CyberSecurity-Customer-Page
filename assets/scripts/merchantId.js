'use strict'

const html = require('../../index.html')

const merchantId = function () {
  const newId = []
  const firstname = []
  const lastname = []

  '#firstname'.push

  if ('#firstname'.length + '' + '#lastname'.length > 32) {
    push()
  }
}
// if firstname.value + '' + lastname.value === 32
// slice -32
// return []

module.exports {
  merchantId
}
